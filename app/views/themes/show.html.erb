<div class='ui padded center aligned teal segment show'>
    <p>
    <strong>name:</strong>
    <%= @theme.name %>
  </p>

    <p>
    <strong>project:</strong>
    <%= @theme.project %>
  </p>

    <%= link_to 'Editar', edit_theme_path(@theme), class: 'ui button' %>
  <%= link_to 'Voltar', themes_path, class: 'ui button' %>
</div>
