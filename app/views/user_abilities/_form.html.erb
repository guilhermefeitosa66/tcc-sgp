<div class="ui form">
  <h4 class="ui dividing header">Informações</h4>
  <%= form_for(user_ability) do |f| %>
    <% if user_ability.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(user_ability.errors.count, "error") %> prohibited this user_ability from being saved:</h2>

        <ul>
        <% user_ability.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

      <div class="field">
        <%= f.label :ability_id %>
        <%= f.collection_select(:ability_id, Ability.available, :id, :name, {}, {:class=>'ui search dropdown'}) %>
      </div>

      <div class="actions">
      <%= f.submit class:'ui teal button' %>
    </div>
  <% end %>
</div>
