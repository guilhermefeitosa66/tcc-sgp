<div class="ui form">
  <h4 class="ui dividing header">Informações</h4>
  <%= form_for([@project, @theme, @user_story, user_story_acceptance_criterion]) do |f| %>
    <% if user_story_acceptance_criterion.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(user_story_acceptance_criterion.errors.count, "error") %> prohibited this user_story_acceptance_criterion from being saved:</h2>

        <ul>
        <% user_story_acceptance_criterion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

      <div class="field">
        <%= f.label :description %>
        <%= f.text_area :description %>
      </div>

      <div class="field">
        <%= f.label :status %>
        <%= f.number_field :status %>
      </div>

      <div class="actions">
      <%= f.submit class:'ui teal button' %>
    </div>
  <% end %>
</div>
